# API keyboard

Global table: keyboard

Calls:

- keyboard.getkey()
- keyboard.getchar()
- keyboard.available()

Input is non-blocking. Special keys may return names like up, down, left, right, home, end, delete.
