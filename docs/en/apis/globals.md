# Exposed Lua Globals

Functions:

- print, tonumber, tostring, type, next, pairs, ipairs, select, error, pcall
- rawget, rawset, rawlen, setmetatable, getmetatable

Module tables:

- io, serial, sys, fs, kfs, process, memory, event, sync, debug, keyboard, format
