# API format

Global table: format

Calls:

- format.encode(table)
- format.decode(string)
- format.is_valid(string)
- format.disk_init()
- format.disk_info()

Constants:

- format.HEADER = PLF:1\n
- format.VERSION = 1
